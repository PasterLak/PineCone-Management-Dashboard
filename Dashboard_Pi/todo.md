stopped/running status badge bei table view ANSTATT blinken (Also vor ID noch status column)

server response bei send once

Sidebar Button net mittig (icon) um 1 px

Header title schoener machen

adopt received configuration mehrmals net nur einmal bei erstem response!

server response wird net mehr angezeigt NACHDEM seite neu geladen oder tab gewechselt idk

settings besser beschreiben wofuer jede settings intervall is und gibt es noch andere?

getValidationRule nachvollziehen + bessere validierung einbauen

remove all css classes, js methods/variables or comments that arent used anynmore after refactoring

was macht genau:

<script>
  window.initialDevices = {{ devices | tojson }};
</script>

improve performance from simulators

improve performance from the table (device manager)
